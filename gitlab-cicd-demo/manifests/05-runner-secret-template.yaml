apiVersion: v1
kind: Secret
metadata:
  name: gitlab-runner-registration-token
  namespace: gitlab-demo
  labels:
    app.kubernetes.io/name: gitlab-runner
    app.kubernetes.io/part-of: gitlab-demo
type: Opaque
stringData:
  token: "REPLACE_WITH_REAL_REGISTRATION_TOKEN"
